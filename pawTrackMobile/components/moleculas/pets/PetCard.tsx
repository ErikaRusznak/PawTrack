import { TextMedium } from "@/components/StyledText";
import { View } from "@/components/Themed";

type PetCardProps = {

};

const PetCard = ({} : PetCardProps) => {

    return (
        <View>
            <TextMedium>pets</TextMedium>
        </View>
    );
};

export default PetCard;